<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      AppCompat-based because the Cast device picker is an AppCompat dialog and
      crashes under a plain platform theme. Everything visible is drawn by
      Compose; this theme only paints the window before the first frame.
    -->
    <style name="Theme.Ember" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="colorPrimary">#FFFF6A3D</item>
        <item name="colorAccent">#FFFF6A3D</item>
    </style>
</resources>
